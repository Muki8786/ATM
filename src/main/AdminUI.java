package main;

public class AdminUI {
    /*public void run()
    {
        LoginUI loginUI = new LoginUI();
        if(loginUI.login())
        {
            int accountNumber = loginUI.getAccountNumber();
            //if(!account.getAdmin())
                //new OptionMenuUI(accountNumber).optionMenu();
            createOptionMenuUI(accountNumber);
            //else
            {
                //createAdminMenu();
            }
        }
        else
        {
            run();
        }
    }

    public static void createOptionMenuUI(int accountNumber)
    {
        new OptionMenuUI(accountNumber).optionMenu();
    }

     */

}
